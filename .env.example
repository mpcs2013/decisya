# Copy to .env (git-ignored). Never commit real values.
POSTGRES_PASSWORD=
REDIS_PASSWORD=
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
BFF_OIDC_CLIENT_SECRET=
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
AI_PROVIDER=ollama            # ollama | openai | anthropic
AI_API_KEY=                   # only for hosted providers; per-tenant BYO keys live in the DB, encrypted
AI_MONTHLY_SPEND_CAP_EUR=20
