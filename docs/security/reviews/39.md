<!-- gate: G6 | verdict: PASS-WITH-NOTES | issue: #39 -->
# Security review: issue #39 (harden hooks and gate checker)

- Date: 2026-09-25
- Reviewer: security-reviewer agent (diff review, gate G6)
- Scope: `git diff origin/main...HEAD` on `issue/39-harden-hooks`: 10 commits (`2d5cb26..89fdc41`), 82 files. The working tree is clean.
- Change class: ci-tooling. This is development tooling, not the Decisya application. ASVS 5.0 is mapped by analogy at section level, as in the G3 threat model.
- Threat model (G3): `docs/security/threat-models/claude-hooks-hardening.md`. Its ids (T-xx, N-xx, H-xx, G4-39-xx) are reused below. New findings from this review are G6-39-xx.
- Method: code reading of every changed file. I could not run Python in this session, so each bypass below was traced by hand through `decide()` (the call chain is given in each finding). Each fix should come with a unit test that reproduces the bypass first.
- **Verdict: PASS-WITH-NOTES.** The skill's rule is that only an open High blocks, and there is none. Items 1 to 8 and 10 meet their requirements, and the recorded deviations are acceptable (see "Rulings on the recorded deviations"). **Item 9 does not fully meet its MUST requirements.** Four Medium bypasses of the secret guard remain:
  - G6-39-01: the heredoc exemption ignores quote and heredoc state from earlier lines.
  - G6-39-02: an unquoted heredoc delimiter lets `$(…)` run inside the exempt body.
  - G6-39-03: `\\` + newline is joined as if it were a continuation.
  - G6-39-04: a grep/rg `-f` inside a short-option cluster slips past the H-06 exemption.

  G6-39-03 and G6-39-04 are new in this diff. The other two are old forms that item 9 was meant to close. G4-39-35, 36, 44 and 47 are therefore FAIL. My recommendation is to fix G6-39-01 to 04 in this PR, then ask me to re-check. If Marco defers them instead, the G7 PR body must not claim that item 9 is done, and a follow-up issue must be linked.
- **Re-check (2026-09-25, commit `27c91af`): verdict stays PASS-WITH-NOTES.** I read the whole fix diff and traced every G6 fixture through the new `strip_exempt_heredocs`, `drop_text_option_values` and `match_rule`. G6-39-01 to 12 and 14 are Fixed. G4-39-35, 44 and 47 now PASS. G6-39-13 is partly closed (see its row). **One new Medium, G6-39-15:** the new line walk skips its heredoc check on any line that *ends* inside a quote, or *starts* inside one. A non-exempt heredoc opened on such a line is never tracked, so its body can again carry an exempt `gh pr create … <<'B'` opener. This is the G6-39-01 class in a narrower form, so G4-39-36 stays FAIL. The fix is small (see the row). Until it lands, the G7 body should not claim item 9 is complete. The remaining open items (G6-39-13a, d and e; actionlint) are Info or conditions for G7. I amended the G3 requirement G4-39-36(5) in place to require a quoted delimiter.

## Evidence summary

- **Code read:** `.claude/hooks/_hooklib.py`, `agent_boundaries.py` and `secret_guard.py`; `.claude/scripts/gates.py`, `lint.py` and `commitlint.py`; `.claude/settings.json`; the `tools:` lines of `backend-dev.md` and `devops.md`; the `ci.yml` `changes` and `claude-config` jobs; `.pre-commit-config.yaml`; `.gitignore`; all six test modules; the 29 commitlint fixtures, their goldens and the two generators; the runbook diff. I also read origin/main's `secret_guard.py`, to tell regressions apart from older gaps.
- **Tests:** 41 unit tests (12 hooks, 3 secret guard, 14 gates, 8 lint, 3 CI, 1 commitlint with 29 goldens). This matches the manifest's count of 41. I did not re-run them. The manifest records them green, together with lint, `commitlint --range`, `gates.py 35/36/41`, the build and 76 tests.
- **Secret scan:** `gitleaks git --log-opts=origin/main..HEAD`: 10 commits scanned, no leaks found.
- **Dependencies:** no NuGet or npm package was added. `conventional-pre-commit` (an external pre-commit repo) was removed and replaced by a local, standard-library-only script. This shrinks the T-16 surface. The removal needs a line in the PR body (G4-39-55).
- **Audit log:** the fixed field set, the canary, the unwritable-log case and rotation are all covered by tests. The log path comes from `Path(__file__)`, never from the payload.

## Findings

| Id | Severity | ASVS 5.0 | file:line | Finding | Evidence | Fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| G6-39-01 | Medium | V13.3, V1.2 | `.claude/hooks/secret_guard.py:143-174` | **The heredoc exemption checks each line in isolation.** `_exempt_delimiter` calls `scan(line)` on one line, so it cannot see quote state or open heredoc bodies from earlier lines. A `gh …`/`git commit … <<X` line that bash treats as *data* still gets the exemption. The lines up to `X` are then skipped, even though bash *executes* them. G4-39-36(1) requires the `<<` to be in unquoted code, and here it is not. The same class of bug existed on origin/main (N-01). | Traced, both allowed. (a) A line inside another heredoc's body: `cat <<'A'` / `gh pr view 1 <<'B'` / `A` / `cat <name>` / `B`. Bash ends heredoc A at `A` and then runs `cat <name>`. The guard exempts line 2 and skips up to `B`. (b) A line inside a multi-line double-quoted string: `echo "` / `gh pr view 1 <<X` / `"; cat <name>` / `X`. | Scan the whole (joined) command once and index the states by absolute offset. Do not exempt when the line starts inside a quote. Also track every heredoc (exempt or not): the body of a non-exempt heredoc can never open an exempt one, so either skip over it, or refuse the exemption while an earlier `<<` is still open. The simplest rule that holds: exempt only when the whole command contains exactly one `<<`. Add both forms as must-deny fixtures. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-02 | Medium | V13.3, V1.2 | `.claude/hooks/secret_guard.py:151` | **An unquoted delimiter is exempt, but bash expands its body.** The regex `<<-?\s*(['\"]?)(\w+)\1` accepts `<<EOF`. With an unquoted delimiter, bash runs parameter expansion, `$(…)` and backticks inside the body. The G3 spec (G4-39-36(5)) explicitly allowed `<<EOF`, so the defect is in my threat model, not only in the implementation. The same gap existed on origin/main. | Traced, allowed: `git commit -F - <<EOF` / `$(cat <name>)` / `EOF`. The file content goes into the commit message, and `git log` (allow-listed) then prints it. | Exempt only quoted delimiters (`<<'EOF'`, `<<"EOF"`, `<<\EOF`). Every must-allow fixture already uses a quoted delimiter, so no test changes. Add the form above as must-deny. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-03 | Medium | V13.3, V1.2 | `.claude/hooks/secret_guard.py:117-126` | **Continuation join is wrong after an escaped backslash (new in this diff).** `join_continuations` removes any `\` + newline whose `\` is in state `c`. But in `\\` + newline, the second `\` is the escaped half of a pair, and the newline is a real newline. The guard joins two lines that bash keeps apart. G4-39-35 requires the join to work "as bash does". | Traced, allowed: `git commit -m x\\` / `bash <<X` / `cat <name>` / `X`. After the join, line 1 reads `git commit -m x\bash <<X`. EXEMPT_OWNER matches `git commit`, so the body is skipped. Bash runs `git commit -m 'x\'` and then feeds `cat <name>` to `bash`. | Walk the command the way `scan` does. When a `\` is in state `c`: if the next character is a newline, drop both; otherwise keep both and advance by 2. Add the form above as must-deny, and `echo \\` + newline + `ls` as a no-join check. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-04 | Medium | V13.3, V2.2 | `.claude/hooks/secret_guard.py:208-236` | **A clustered short option bypasses the file-option rule (new in this diff, a regression against origin/main).** The `FILE_OPTIONS` check compares whole tokens, so `-rf`/`-rFf` is not seen as a file-valued option. For grep and rg, the first positional is then dropped as the "pattern". But in this command it is the `-f` file. G4-39-44 requires grep `-f` to be scanned always. | Traced, allowed: `grep -rf <name> .`. The tokens `-rf`, `<name>` and `.` reduce to `grep -rf .`. Bash reads the patterns from the file and searches the tree, and each line of the file matches itself, so the whole file is printed (`-rFf` makes the match literal). On origin/main this command was denied. | Treat any short cluster `^-[A-Za-z]+$` that contains a file-valued letter as a file option: `f` for grep/rg/git grep, `F` for git commit and gh. Then return `cmd` unchanged (no exemption). More simply, give up the exemption whenever a short cluster longer than two characters is present. Add `grep -rf`, `grep -rFf` and `rg -uf` as must-deny fixtures. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-05 | Low | V13.3 | `.claude/hooks/secret_guard.py:48-54` | A glob suffix directly after the name is missed: `.env*`, `.env?` and `.env[.]local`. `*`, `?` and `[` are not in the trailing set, and patterns 1 and 2 only cover globs *inside* `.env`. `settings.json` denies only `cat .env*` and `type .env*`. This gap also existed on origin/main, and the G3 requirements did not list it. | Traced, allowed: `head -n 50 <name>*`, `less <name>?`. | Add `*`, `?` and `[` to `_TRAIL_CHARS`. Add both forms as must-deny fixtures. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-06 | Low | V15.3 | `.claude/hooks/secret_guard.py:56` | **The compose pattern is quadratic.** In `docker[- ]compose\b[^\n;&\|]*\b(?:config\|convert)\b`, the greedy class runs to the end of the line from every `docker compose` start and then backtracks. That costs O(starts × line length). A line of about 250 KiB made of `docker compose ` repeated, followed by a second line `docker compose config`, would take tens of seconds (estimate). That exceeds the 10 s hook timeout, and a timeout is a non-blocking error, so the call goes ahead (H-02, even for listed agents under the 256 KiB cap). The random-printable timing test never contains this prefix. G4-39-47 requires every regex to be linear. | Traced by reasoning. The timing is an estimate: I could not run it. | Bound the class, for example `[^\n;&\|]{0,256}`, or split the command on `[\n;&\|]` and test each segment with `in`. Add an adversarial timing test: 256 KiB of `docker compose ` repeated, under 1 s. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-07 | Low | V13.3 | `.claude/hooks/secret_guard.py:59` | EXEMPT_OWNER accepts **any** `gh` subcommand, including ones that store the heredoc body as code. `gh alias set --shell <a> -` reads its expansion from stdin, and `gh <a>` then runs it through the shell. The two-step read never names the file in a scanned position. | Traced from gh's documented `-` stdin form for `alias set` (confirm it with a probe): `gh alias set --shell x - <<'E'` / `cat <name>` / `E`, then `gh x`. | Narrow the owner to `gh (pr\|issue) (create\|comment\|edit)\b` (G4-39-38 needs only these) and `git … commit`. Also add `gh alias set` and `gh extension install` to the agent command policy (G6-39-09). | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-08 | Low | V13.3 | `.claude/hooks/secret_guard.py:48` | The leading boundary misses `@`, `:` and a value glued to a short option. Allowed: `curl -d @<name> …` (upload), `gh issue create -t x -F<name>` (publishes the content), `git commit -F<name>`, `grep -f<name> x`, `rg -g<name> -uu KEY`, `git show HEAD:<name>`. These gaps also existed on origin/main, and the G3 lead set did not include them. | Traced. | Add `@` and `:` to the lead set, and a fixed-width lookbehind `(?<=\s-[A-Za-z])` for glued short options. Neither change matches `process.env` or `import.meta.env`. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-09 | Low | V8.2, V15.2 | `.claude/hooks/agent_boundaries.py:27-35` | The agent command policy matches prefixes on the raw text. It misses global flags before the verb (`gh -R o/r issue close 5`, `gh --repo o/r run delete 1`), `dotnet.exe package add` (Windows host), and quoted verbs (`dotnet "package" add`). Package-fetching paths outside `dotnet` are not covered either: devops's `Bash(aspire *)` includes `aspire add`, `Bash(pre-commit *)` includes `pre-commit try-repo <url>` (runs remote code), and `gh extension install` is not denied. The hook is a guardrail (T-01, T-02), so this is residual rather than a regression. | Code reading. | Match against `secret_guard.normalise()` of each simple command. Allow `(?:\.exe)?` and `(?:\s+-{1,2}\S+(?:\s+\S+)?)*` between the program and the group. Add `aspire add`, `pre-commit try-repo\|autoupdate` and `gh (extension\|ext) install\|alias set`. Follow-up, not required for this PR. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-10 | Low | V15.3 | `.claude/hooks/agent_boundaries.py:20-21`, `secret_guard.py:41-42` | `import _hooklib` runs at module level, before the payload is parsed. If the import fails, Python exits 1, which is a non-blocking error, so the call is allowed for every agent. G4-39-01 names "importing a shared helper" as an error that must deny for a listed agent. On the host an agent with Bash can break the helper (T-01). In the sandbox `.claude/` is read-only. | Code reading. | Wrap the import. On failure, parse stdin with `json` and deny any non-empty `agent_type` (the G4-39-02 "no source" rule). The main session stays open. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-11 | Info | V8.2 | `.claude/scripts/lint.py:201-215` | `wildcard_grant` accepts a verb that ends in `*` after any word prefix. So `Bash(dotnet p*)` (covers `package`) and `Bash(gh issue c*)` (covers `close` and `comment`) pass the lint. | Code reading. No current agent file does this. | Require the verb, with `*` stripped, to be at least 3 characters long, or check it against an allow-set of known verbs. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-12 | Info | V16.3 | `.claude/scripts/gates.py:153` | The lane check matches the manifest's path string, not the resolved path. A symlink inside `docs/security/` that points to a file in another lane would pass. Only T-01-level Bash access can create one. | Code reading. | Match the lane globs against `resolve().relative_to(ROOT).as_posix()`. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-13 | Info | — | manifest, tests | Evidence gaps. (a) G4-39-04: no sandbox probe of the `$CLAUDE_PROJECT_DIR` hook path is recorded (host only). (b) G4-39-31: no `git check-ignore` red/green run. (c) Item 6: no red run against the old `gates.py` (the "red" tests are negative cases on the new code). (d) G4-39-41: the review 35 N-02 forms `.\env`, `."env"`, `.?nv` and `'*env'` are caught by the code but have no fixtures. (e) `footer-max-line-length` is never triggered by a real footer (`footer-101` parses as body). (f) The manifest and commit `3ef80bf` label the hook-path fix G4-39-05, but it is G4-39-04. (g) `make_fixtures.py` points to a `README.md` that does not exist, and the `commitlint.config.mjs` that `make_golden.mjs` uses is not described. | Reading the manifest and fixtures. | Add the fixtures in (d) and (e). Record (a) at the next sandbox run. Fix the references in (f) and (g). None of this blocks. | Re-check (`27c91af`, `77eb05e`): (b) closed (`git check-ignore` gives `.gitignore:25`); (c) closed (the new tests against origin/main's `gates.py` give 11 failures and 1 error out of 16); (f) the manifest label is fixed, and `3ef80bf` keeps the old one because there is no history rewrite (accepted); (g) the README reference is replaced by the header of `make_golden.mjs`. Still Open, Info, none blocks: (a) the sandbox probe (Marco, needs Docker); (d) fixtures for `.\env`, `."env"`, `.?nv` and `'*env'` (the code catches all four); (e) a real footer-overflow golden. |
| G6-39-14 | Info | V15.2 | `.claude/scripts/commitlint.py:53-62` | `git_strip` does not remove leading blank lines or collapse repeated blank lines, as git's `strip` does. The mirror can be stricter than CI on unusual messages. CI stays authoritative (H-09). | Code reading. | Optional: mirror both rules, with a fixture. | Fixed (`27c91af`; re-checked 2026-09-25) |
| G6-39-15 | Medium | V13.3, V1.2 | `.claude/hooks/secret_guard.py` `strip_exempt_heredocs` (the `if start_mode != "c" or mode != "c" … continue` line) | **New at the re-check, a residual of the G6-39-01 fix.** A line that starts or ends inside a quote skips the heredoc check completely. If that line opens a *non-exempt* heredoc, the heredoc is not tracked, so its body can hold an exempt opener. The guard then skips lines that bash executes. G4-39-36(1) requires that no line inside another heredoc's body can open an exempt heredoc. | Traced, allowed: `echo "x` / `"; cat <<A` / `gh pr create --body-file - <<'B'` / `A` / (read of the file) / `B`. Line 2 starts in a quote, so `<<A` is missed. Line 3 is exempted, and its "body" swallows `A` and the read. In bash, line 3 is A's body, A ends at line 4, and line 5 runs. A second form: `cat <<A "` / `"` / the same three lines (the opener line ends in a quote; bash starts A's body after the line that completes the command). | Compute the line's states from `start_mode` (`scan_line(line, start_mode)`). If any `<<` in state `c` (not `<<<`) is present, exempt it only when `start_mode == mode == "c"` and `_exempt_delimiter` accepts it. Otherwise `out.extend(lines[i:])` and stop, which is the same rule as for any other non-exempt heredoc. Add both forms as must-deny fixtures. | Open |

## Requirement status (G4-39-01 to 58)

PASS: met, with evidence. PARTIAL: met with a gap (see the finding). FAIL: a MUST is not met. DEV-OK: a recorded deviation that I accept (see Rulings).

| Req | Status | Evidence / note |
| --- | --- | --- |
| 01 | PASS | `_hooklib.read_payload` fails open on bad stdin. The main session and unlisted agents are unchanged. Listed agents get a JSON deny (`agent_boundaries.py:100-113`, `secret_guard.py:274-291`). An import failure still fails open (G6-39-10). Re-check: an import failure now denies any subagent (G6-39-10 fixed). |
| 02 | PASS | `listed_agents()` falls back to frontmatter names, and to "every agent is listed" when both sources fail (`_hooklib.py:84-101`). Tested. |
| 03 | PASS | The reason text is exact (`_hooklib.py:114-116`). stderr gets only the exception class. The test checks that the value `12345` is absent. |
| 04 (SHOULD) | PARTIAL | `settings.json` uses `"$CLAUDE_PROJECT_DIR/.claude/hooks/…"`. The host probe is recorded, the sandbox probe is not (G6-39-13a). |
| 05 | PASS | 256 KiB and 4 KiB caps deny listed agents (`agent_boundaries.py:58-59, 81-82`, `secret_guard.py:278-281`). The main session is scanned. The 1 MiB test runs under 1 s. |
| 06 | PASS | Both directions, with file:line, and the name/stem check (`lint.py:218-239, 253-256`). Tested. |
| 07 | PASS | One `validate_boundaries` serves both the lint and the hook (`_hooklib.py:51-68`). 8 schema cases are tested. |
| 08 | PASS | `settings.json:25-27`. |
| 09 | PASS | Runbook, "What the hooks do". |
| 10 | PASS | Live probe (settings approach, mid-pattern wildcard honoured), recorded in the manifest. |
| 11 | PASS | `devops.md:4` gets explicit `gh issue view/list/status` and `gh run view/list/watch`. |
| 12 | PASS | `lint.py:196-215`. A gap in partial verbs: G6-39-11. |
| 13 | DEV-OK | Enforced by the hook instead of `settings.json` (ruling 3). The hook denies a superset (`agent_boundaries.py:32-34`). One live probe of the hook path is recorded (`dotnet package add`). Bypass residuals: G6-39-09. |
| 14 | PASS | `backend-dev.md:4` and `devops.md:4` list explicit dotnet verbs. |
| 15 | PASS | None of those forms is granted, and the hook denies all of them (`agent_boundaries.py:28-31`). |
| 16 | DEV-OK | As 13. The runbook states that the main session is not affected (H-08 no longer applies). |
| 17 | PASS | `utf-8-sig` for both the manifest and the artefact (`gates.py:67, 97`). Tested. |
| 18 | DEV-OK | Own-line anywhere, with no competing copy (ruling 1). |
| 19 | PASS | `gates.py:109-111`: any complete same-gate, same-issue comment anywhere makes the artefact AMBIGUOUS. Tested for fenced, own-line and mid-sentence copies. |
| 20 | PASS | `KNOWN_VERDICTS`. UNKNOWN VERDICT and N/A-without-reason are tested. |
| 21 | PASS | `gates.py:138-146`. The file is not read when the path is invalid. The 5 red paths are tested. |
| 22 (SHOULD) | PASS | Lane check from `boundaries.json` (`gates.py:147-154`). Tested. A symlink note: G6-39-12. |
| 23 | PASS | `block-hidden-in-fence-then-pass`, `indented-pass-only`, `bom-first-line` and the path cases are in `test_gates.py`. |
| 24 | PASS | `ROOT/.agent-logs/hooks.jsonl`, computed from `__file__`, created on first write, git-ignored. |
| 25 | PASS | `deny`, `deny-error`, `allow-error` (main session, secret guard). |
| 26 | PASS | The field allow-list is exact. `path` appears only for write denies, and only as a relative path, `<outside-repo>` or `<too-long>`. The set is asserted in a test. |
| 27 | PASS | `ensure_ascii`, a 256-character cap on every field, one append write. |
| 28 | PASS | Rotation above 1 MiB to `.1`. Tested. |
| 29 | PASS | `audit()` has its own try/except and runs after `emit()`. The unwritable-log test passes for both hooks. |
| 30 | PASS | `test_log_never_contains_command_text`. |
| 31 | PASS | `.gitignore:25`. The check-ignore run is not recorded (G6-39-13b). |
| 32 (SHOULD) | PASS | Runbook "Audit log" section. The `host-review.py` count was not done, which is optional (ruling 4). |
| 33 | PASS | `ci.yml:44-49`: the token comes from `openssl rand -hex 16`, is printed only on the markers, and has no output writes in between. Under `bash -e`, a failure of `openssl` aborts the step. |
| 34 | DEV-OK | The bash test exists, 3 cases. actionlint was not run (ruling 4). |
| 35 | PASS | Re-check: G6-39-03 fixed. Continuations are joined as bash joins them, never inside a body, and two backslashes followed by a newline are not joined. |
| 36 | FAIL | Re-check: conditions 2 and 5 now PASS (G6-39-07 and 02 fixed; condition 5 amended in G3 to require a quoted delimiter). The G6-39-01 forms are fixed, but condition 1 still fails through G6-39-15 (a heredoc opened on a line that starts or ends inside a quote is not tracked). |
| 37 | PASS | Every listed form is in `MUST_DENY`, and each one traces to a deny. |
| 38 | PASS | `MUST_ALLOW`: commit, `git -c`, `cd &&`, `gh pr create` heredocs. |
| 39 | PASS | The lead and trailing sets are as specified, and the `.env.example` whole-name rule holds. Gaps beyond the spec: G6-39-05, G6-39-08. |
| 40 | PASS | All four patterns are present. Performance: see 47. |
| 41 | PARTIAL | The normalised view is correct. Fixtures are missing for 4 of the review 35 N-02 forms (G6-39-13d). |
| 42 | PASS | Residuals are listed in the docstring. |
| 43 | PASS | The single-simple-command, program, option and `$`/backtick conditions are implemented. |
| 44 | PASS | Re-check: a file option inside a short cluster disables the exemption (`_short_file_option`; G6-39-04 fixed). |
| 45 | PASS | All 10 forms are in `MUST_DENY`. |
| 46 | PASS | All forms are in `MUST_ALLOW`. The runbook records the remaining false positives. |
| 47 | PASS | Re-check: compose is matched per segment (linear). There is an adversarial timing test for compose, continuation, quote, heredoc and backslash floods (G6-39-06 fixed; the manifest records 95 s before, 0.4 s after). |
| 48 | PASS | 10 error rules. The goldens confirm them. |
| 49 | PASS | Both warnings exit 0 (`body-no-blank`, `footer-no-blank`). |
| 50 | PASS | The formatter output matches the goldens byte for byte (per the manifest and `test_commitlint.py`). |
| 51 | PASS | 29 goldens from commitlint 19.2.1 and config-conventional 19.1.0 cover the required list. Footer-overflow gap: G6-39-13e. |
| 52 | PASS | The header regex and the quote-stripping case check are ported. The goldens settled the edge cases. |
| 53 | PASS | Comment lines, the scissors line and trailing blank lines are handled, and the is-ignored defaults are ported. Info: G6-39-14. |
| 54 | PASS | Standard library only. The hook mode reads only its file argument. `subprocess` appears only in `--range` mode (git with a fixed argument list, no shell), which is acceptable because G4-39-56 requires that mode. |
| 55 | PASS | `.pre-commit-config.yaml`: local `commitlint` hook, `default_install_hook_types`. The Done-when probe is recorded. The PR body must note that `conventional-pre-commit` was dropped. |
| 56 (SHOULD) | PASS | `--range`. It is clean on `origin/main..HEAD`. |
| 57 | PASS | Error and log text carry no command text. Test names are built at run time. gitleaks is clean. |
| 58 | PASS | Lint is green (manifest). `claude-config` runs the unittest suite (`ci.yml:143-144`). T-11 and T-15 are unchanged, and each new test step widens the PR-controlled code that job runs (FU-2). |

## Rulings on the recorded deviations

1. **G4-39-18: own-line anywhere instead of line 1. Accepted.** The purpose of G4-39-18 was to close N-05 (a fenced BLOCK next to a PASS, and indented verdicts). Own-line (column 0, outside fences) plus G4-39-19's "any other complete copy for the same gate and issue makes the artefact AMBIGUOUS" closes both directions, and the tests show it. A strict line-1 rule would break the G1 and G5 verdicts, which share one requirements file. The lane check (G4-39-22) limits who can write the artefact at all. What remains is cosmetic: a reader who looks only at line 1 may miss the verdict. The skill still says "first line", and security-reviewer keeps writing it there.
2. **SAFE_PREFIX also accepts `git add <paths> &&` (G4-39-36). Accepted.** The prefix is not removed from the scanned text: only the heredoc body is exempt. So a `git add` that names a secret file is still denied. The path class `[^\s;&|<>$`()]+` allows no substitution, redirection or chaining, and `git add` does not read the heredoc. This deviation does not widen G6-39-01, 02 or 07. Those come from the line-local scan, the unquoted delimiter and the `gh` owner.
3. **G4-39-13 and 16: hook-enforced denies for project agents instead of `settings.json` denies (Marco's decision). Accepted.** For subagents, the hook on `agent_type` is at least as strong as a prefix deny rule: `tools:` patterns may not bind subagents (T-02), and a settings deny is also a prefix match. The hook denies a superset of the G3 list, and the change removes H-08 (the main session locked out of commands it may legitimately run). The costs I accept:
   - built-in and plugin agents are not bound (T-07b, already accepted);
   - a hook that cannot start fails open (H-02), where a settings deny would still hold;
   - the regex has prefix bypasses (G6-39-09).

   In the sandbox's headless run nothing can be approved at a prompt, and commands that are not allow-listed are refused, so the main session's exposure there does not change.
4. **actionlint not run, and the optional `host-review.py` deny count skipped. Accepted, with one condition.** The workflow edit is small: four `run` lines inside an existing step and one new `run` step. The bash test covers the logic, and PyYAML parses the file. Condition for G7: the PR's own CI run must be green for the `changes` and `claude-config` jobs, which exercises both edits on GitHub's parser and runner. Run actionlint when Docker is available, or add it to CI under #28. The `host-review.py` count was an optional SHOULD. Skipping it keeps `.devcontainer/**` unchanged, which is the safer choice.

## ASVS 5.0 chapter walk (tooling analogy)

- **V1 Encoding and sanitization:** PASS-WITH-NOTES. The CI workflow-command injection is closed (G4-39-33). The secret guard's shell-matching flaws are G6-39-01 to 04 and G6-39-07. They are matching bugs, not an injection sink. Re-check: all fixed in `27c91af`, except that G6-39-15 remains open.
- **V2 Validation:** PASS. The `boundaries.json` schema, the artefact path confinement and the payload type checks all deny for listed agents.
- **V8 Authorization:** PASS-WITH-NOTES. The two-way lint and the agent command policy are in place. G6-39-09 and G6-39-11 are guardrail residuals.
- **V13 Configuration and secrets:** PASS-WITH-NOTES. gitleaks is clean and no secret is in the diff. The guard's Medium and Low findings were G6-39-01 to 08, all fixed at the re-check. G6-39-15 (Medium) is open. The boundary is still the sandbox, which has no secrets mounted.
- **V15 Secure coding and dependencies:** PASS-WITH-NOTES. No packages were added, and one external pre-commit repo was removed. Fail-closed is in place. The import path (G6-39-10) and the regex timeout (G6-39-06) were fixed at the re-check.
- **V16 Logging:** PASS. The audit log has an allow-listed schema, no command text, ASCII-escaped JSON, field caps, rotation and isolation from the decision. H-04 (tampering) is accepted.
- **V3 to V7, V9 to V12, V14, V17:** N/A. No endpoint, session, token, crypto, transport or data-protection code changes. **AI lanes:** N/A for the product. For the tooling, prompt injection remains T-10 (accepted).

## Follow-ups

| Id | Proposed action | Covers | Blocks #39? |
| --- | --- | --- | --- |
| FU-39-A | Fix in this PR (recommended), or open an issue and leave item 9 unclaimed in G7 | G6-39-01, 02, 03, 04 (and G4-39-35, 36, 44). **Re-check: done in `27c91af`.** What remains is G6-39-15 (G4-39-36(1)), with the same recommendation. | No, by the skill's rule (Medium). The Done-when for item 9 is not met until G6-39-15 is fixed. |
| FU-39-B | Same PR if cheap, otherwise the same follow-up | G6-39-05, 06, 07, 08, 10. **Re-check: done in `27c91af`.** | No |
| FU-39-C | #52 or a new tooling issue | G6-39-09, 11, 12, 14 (**re-check: done in `27c91af`**); actionlint in CI (#28) is still open | No |
| FU-39-D | Before G7 | Still open: G6-39-13 (a) the sandbox probe (Marco, at the next sandbox run), (d) the four N-02 fixtures, (e) the footer-overflow golden; the PR-body line for dropping `conventional-pre-commit`; ruling 4's condition (green `changes` and `claude-config` jobs on the PR) | No |
