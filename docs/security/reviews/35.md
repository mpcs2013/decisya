<!-- gate: G6 | verdict: PASS-WITH-NOTES | issue: #35 -->
# Security review: issue #35 (Claude Code agents, skills, hooks and CI gates)

- Date: 2026-09-23
- Reviewer: security-reviewer agent (diff review, gate G6)
- Scope: `git diff origin/main...HEAD` (13 commits, `f611ea6..da9d865`, 51 files) plus the uncommitted `docs/ai/pipeline/35.md` and `docs/security/threat-models/claude-config.md`. The working-tree deletion of `Phase-0-PlatformFoundations.ps1` is Marco's own and out of scope.
- Change class: ci-tooling. This is development tooling, not the Decisya application. ASVS 5.0 is mapped by analogy at section level, as in the G3 threat model. N/A means no control fits.
- Threat model (G3): `docs/security/threat-models/claude-config.md`. Its ids (T-xx) are reused below. New findings from this review are N-xx.
- **Verdict: PASS-WITH-NOTES.** There is no open High without a fix or a linked follow-up. The one High from G3 (T-12) is mitigated in `da9d865` by an `author_association` guard, and the remaining T-12 work goes to #28. #28's body does not list that work yet, so a comment on #28 is needed (see Follow-ups). Every other open item is Medium or Low.

## Evidence summary

- **Secret-guard probes:** `secret_guard.py` was re-probed with 40 commands. The payloads were built at run time from placeholders in a scratchpad script and piped to the hook as PreToolUse JSON.
  - Every form that `da9d865` claims to fix is now blocked.
  - The known residuals (names built at run time) are still allowed, as documented.
  - New bypasses: the narrowed heredoc exemption (N-01), and a few literal-name forms (N-02).
- **gates.py probes:** `gates.py` was checked by calling `check_gate` and `verdict_lines` on 12 scratch artefacts (N-05).
- **Lint:** `python .claude/scripts/lint.py` reports `claude-lint: OK (9 agents, 13 skills)`.
- **Gate check:** `python .claude/scripts/gates.py 35` passes G0 to G5. Before this file existed, G6 failed with MISSING and G7 with "not completed".
- **Secret scan:** `gitleaks git --log-opts=origin/main..HEAD` scanned 13 commits and found no leaks.
- **Dependencies:** no NuGet or npm package is added. `dotnet-tools.json` pins `dotnet-ef` 10.0.12 with `rollForward: false`. `dotnet list package --vulnerable` and `npm audit` are unaffected.
- **T-02 re-observed:** this run's `tools:` line allows only `Bash(git fetch|diff|log|status*)`, yet `cat`, `sed`, `python`, `gh`, `gitleaks` and `mkdir` ran.

## Findings

| Id | Severity | ASVS 5.0 | file:line | Finding | Evidence | Fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T-03 | Medium | V8.2, V15.2 | `.claude/settings.json:10`; `.claude/agents/devops.md:3` | Allow rules ran package-fetching commands with no prompt. Devops had unscoped `gh` and `docker`. | `dotnet add*` is removed. `dotnet new*` is narrowed to `dotnet new list*`. Devops gets `gh issue/pr view/list/checks/run/workflow view` and `docker compose up/down/ps/logs` and `docker ps/logs`. `gh api`, `gh pr merge`, `gh secret` and `gh repo` are gone. | Done in `da9d865`. Residual in N-04. | Fixed |
| T-04 | Medium | V13.3 | `.claude/hooks/secret_guard.py:17-32` | The guard missed quote-split names, globs, `docker compose config`, `.\` paths and heredocs fed to interpreters. | Now blocked: `.e''nv`, `.en?`, `*.env`, `.e*`, `docker compose config`, `cat .\.env`, `bash <<EOF … EOF` and `python - <<'X' … X`. Commit and `gh` heredocs that mention the name are still allowed (no false positive). The documented residual is still allowed: `$(printf '.%s' env)`, `python -c "open('.'+'env')"`, `grep -r PASSWORD .` and `ls $APPDATA/Microsoft/User*`. | Listed forms fixed in `da9d865`. Residual goes to the sandbox follow-up (FU-1). | Fixed (listed forms); residual Open-follow-up (FU-1) |
| T-05 | Low | V13.3 | `.claude/settings.json:31-32` | `Read` deny covered only the root `.env*`. | `Read(./**/.env)` and `Read(./**/.env.*)` are added. `.env.example` is readable only through Bash (the guard allows it). | Done in `da9d865`. | Fixed |
| T-09 | Medium | V16.3, V8.2 | `.claude/scripts/gates.py:79-81, 96-128` | (a) The checker took the last verdict line anywhere in the artefact. (b) Skip approvals are free text that anyone can write. | (a) Probes: a BLOCK first line with a quoted PASS in a fence or mid-sentence gives BLOCK. A second own-line PASS gives AMBIGUOUS. A wrong issue number fails. (b) A skip note reading `approved: Marco 2099-01-01` still passes. | (a) Done in `da9d865` (exactly one own-line verdict outside fences). Residual in N-05. (b) Bind approvals to Marco's GitHub identity (FU-4). | (a) Fixed; (b) Open-follow-up (FU-4) |
| T-10 / T-01 docs | Medium | N/A, V13.1 | `CLAUDE.md:26-27` | No rule said repository and GitHub content is data. No document said the hooks are guardrails. | Both lines are present. `secret_guard.py:5-9` docstring says the same. | Done in `da9d865`. T-10 itself stays Accepted (inherent). | Fixed (docs); T-10 Accepted |
| T-12 | High (Medium after guard) | V13.3, V15.2, V8.2 | `.github/workflows/claude-review.yml:15-19` | Any commenter could start a job that holds `ANTHROPIC_API_KEY`, `pull-requests/issues: write` and `id-token: write`, running instructions from the PR tree. | The guard is added: `contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)`. `comment.author_association` exists on both `issue_comment` and `pull_request_review_comment`, so outside commenters can no longer trigger the job. Still open: (b) actions not SHA-pinned; (c) instructions, settings and hooks load from the PR head; (d) `issues: write` may not be needed; no same-repository head check. After the guard, exposure needs a maintainer to comment `@claude` on an untrusted PR. | Remainder goes to #28 (FU-3). The YAML comment says "tracked in #28", but #28's body (0.16 CI hardening) does not mention `claude-review.yml`. Add the items to #28. Until then, never `@claude` on a PR Marco did not write. | Guard Fixed; remainder Open-follow-up (#28, FU-3) |
| T-01 | Medium | V8.2 | `.claude/settings.json:38-52`; `.claude/hooks/agent_boundaries.py` | The write-boundary hook does not cover Bash writes. | Unchanged. Documented as a guardrail (CLAUDE.md:27). | Sandbox follow-up (FU-1). | Open-follow-up (FU-1) |
| T-02 | Medium | V8.2, V13.1 | `.claude/agents/*.md:3` | Pattern-scoped `Bash(...)` in agent `tools:` does not restrict the subagent. | Re-observed in this run (see Evidence summary). | Verify in default permission mode. Then sandbox (FU-1), or add `permissions.deny` per lane. | Open-follow-up (FU-1) |
| T-11 | Medium | V15.2, V13.1 | `.github/workflows/ci.yml` `claude-config` job | A PR runs its own `lint.py`/`gates.py`, so it controls its own gate. | Unchanged. | Run the scripts from `origin/$GITHUB_BASE_REF` and require the status check on `main` (FU-2). | Open-follow-up (FU-2) |
| T-15 | Low | V8.2 | `.github/workflows/ci.yml:8-10` | `claude-config` inherits `security-events: write` while it runs PR-controlled Python. | Unchanged. | Add job-level `permissions: { contents: read }` (FU-2). | Open-follow-up (FU-2) |
| T-16 | Medium (actions with secrets) / Low | V15.2 | `.github/workflows/*.yml`, `.pre-commit-config.yaml` | Actions and pre-commit repositories are pinned by mutable tag. | Unchanged. | #28 (FU-3). | Open-follow-up (#28) |
| T-06, T-07, T-14, T-18 | Low | V15.3, V8.2, V1.2, V16.3 | hooks, `lint.py:223-227`, `ci.yml` `changes` job | Hooks fail open. Agents missing from `boundaries.json` are unrestricted, and the lint checks only one direction. PR file names reach workflow stdout. No hook audit log. | Unchanged since G3. | Low-priority hardening bundle (FU-5). | Open-follow-up (FU-5) |
| N-01 | Low (was Medium; re-check 2026-09-23 after `839c893`) | V13.3, V1.2 | `.claude/hooks/secret_guard.py:28-32, 42` | **New with `da9d865`: the heredoc exemption covers other commands on the same line.** `[^\n]*?` between `git commit`/`gh` and `<<` can span `;`, `&&` and `\|`. The start anchor `(?:^\|[;&\|\n])` also matches a `gh` that comes *after* another heredoc on the line. So the body of a heredoc fed to `bash`/`python` is stripped before matching. This is not a regression: before `da9d865` every heredoc body was stripped. But it defeats fix T-04(a). | All allowed, each with the literal name in the body: `gh pr view 1; bash <<'X'` + `cat <name>`; `git commit --allow-empty -m x && python - <<'X'` + `open('<name>')`; `gh pr view 1 \| sh <<'X'` + …; `bash <<'A'; gh pr view 1 <<'B'` + `cat <name>` (the regex strips from the first newline to `B`, which includes A's body). | Anchor the exemption to a line that is only a `git commit` or `gh` command with one heredoc. For example, make the prefix `(?:^\|\n)\s*(?:git\s+(?:-c\s+\S+\s+)*commit\|gh)\b[^\n;&\|<]*<<-?…`, so any `;`, `&`, `\|` or second `<<` on that line disables the exemption. Add these four probes as unit cases. Fix in this PR or in FU-6. **Re-check (`839c893`, line-based `strip_exempt_heredocs`, `secret_guard.py:30-43`):** the four cases above now block. So do `gh … <<X \| bash`, a second heredoc on a later line, and a read after the terminator. The false-positive checks still allow a commit or PR-body heredoc, `git -c … commit` and `cd … && git commit`. **Still allowed**, because the check is textual and does not parse the shell: (1) a backslash continuation into the `gh` line, `python3 - \` + newline + `gh <<'X'` + body (bash joins the lines, so the heredoc feeds python), and the same with `bash -s \`; (2) `gh … <<X` inside a comment, `echo hi # ; gh pr view 1 <<X`, where there is no real heredoc and bash runs the "body" lines as commands; (3) the same inside a quoted string, `echo "; gh pr view 1 <<"X"` or the single-quoted form. A missing terminator is not a bypass (bash treats the rest as data). **Remaining fix:** exempt only when the heredoc line is the first line of the command, or the line before it does not end in `\`, and nothing before `<<` on that line contains `#`, `'`, `"` or `` ` ``. Simplest: exempt only when the whole command is a single `git commit`/`gh` invocation whose first line matches `^\s*(?:git…commit\|gh)\b[^;&\|<#'"`\\\n]*<<…$`, and scan anything after the terminator line. | Re-check: direct forms Fixed; residual (1)-(3) Open-follow-up (FU-6) |
| N-02 | Low | V13.3 | `.claude/hooks/secret_guard.py:18-22` | Regex false negatives where the name is still literal or nearly so. The trailing-boundary set `[\s'";\|&)>]` misses `` ` ``, `,`, `}`. The leading set misses `{`, `(` and `` ` ``. There is no pattern for `.\env`, `."env"`, `.[e]nv`, `.?nv`, `docker-compose config` or `docker compose -f x config`. `.env.example.bak` is allowed by the `example\b` look-ahead. | All allowed: ``cat `echo <name>` ``, `cat {<name>,}`, `cat .\env`, `cat ."env"`, `cat .[e]nv`, `cat .?nv`, `docker-compose config`, `docker compose -f x.yml config`, `find . -name '*env' -exec cat {} +`, `gc ('.e'+'nv')`. | Widen both boundary sets to include `` ` ``, `{`, `}`, `(`, `,`. Add `\.[\\'"?\[]` near `env`, `docker[- ]compose\b[^\n;&\|]*\bconfig\b`, and `(?!example$)`-style anchoring. Accept that run-time construction stays out of scope (FU-1). FU-6. | Open-follow-up (FU-6) |
| N-03 | Info | V13.3 | `.claude/hooks/secret_guard.py:17-24` | False positives. Inline commit messages and searches that mention the names are blocked. | Blocked: `git commit -m 'docs: explain secrets.json'`, `git log --grep=UserSecrets`, `grep -rn "\.env" docs`. | Accept: the workaround is `git commit -F - <<EOF` (exempt). Record it in `docs/runbooks/issue-pipeline.md` in FU-6. | Open-follow-up (FU-6) |
| N-04 | Low | V8.2, V15.2 | `.claude/agents/devops.md:3`, `.claude/agents/backend-dev.md:3`, `.claude/settings.json:15-17` | Residual T-03 surface. Agent `Bash(dotnet *)` still covers `dotnet add package` and `dotnet new install`. These now prompt, because the settings allow is gone. `gh issue *` covers `gh issue delete/transfer/edit`, and `gh run *` covers `rerun/cancel/delete`. `docker compose up` runs compose files that devops can write (`deploy/**`), which can bind-mount host paths. The allow-listed `git diff*` and `git log*` accept `--output=<path>`, a prompt-free file write that the boundaries hook does not see. | Code reading. `settings.json` has no `--output` deny. | Narrow `gh issue *` to `gh issue view*\|list*\|comment*` and `gh run *` to `gh run view*\|list*\|watch*`. Add deny rules `Bash(git diff*--output*)` and `Bash(git log*--output*)`. Drop `dotnet add`/`new install` from agent patterns. FU-5. | Open-follow-up (FU-5) |
| N-05 | Low | V16.3 | `.claude/scripts/gates.py:109-128, 90-92` | Verdict parsing edge cases. (1) A verdict line inside a fence is ignored, so an artefact with a fenced BLOCK and an own-line PASS passes. Only the author can do this, and it contradicts the skill's "first line" wording. (2) A 4-space-indented (code-block) verdict counts. (3) A UTF-8 BOM before line 1 gives NO VERDICT (fails closed, but can confuse Windows editors). (4) An absolute or `..` artefact path in the manifest reads outside the repository (read-only, as T-17 noted). | Probes: `block-hidden-in-fence-then-pass` gives PASS. `indented-pass-only` gives PASS. `bom-first-line` gives NO VERDICT. Scratchpad absolute paths were read. | Require the verdict on the first non-empty line (strip BOM). Reject artefact paths that do not resolve under `ROOT`. FU-5. | Open-follow-up (FU-5) |
| N-06 | Info | V13.1 | `docs/ai/pipeline/35.md`, `docs/security/threat-models/claude-config.md`, this file | The G3, G6 and manifest artefacts are uncommitted. The CI `claude-config` "Pipeline gates" step reads the PR tree, so it fails until they are committed. | `git status --porcelain` shows `??` for both directories. | Commit the manifest, threat model and this review before opening the PR (orchestrator, G7). | Open |

## ASVS 5.0 chapter walk (tooling analogy)

- **V1 Encoding and sanitization:** PASS. `HEAD_REF` reaches the shell through `env:`, is quoted, and only digits are extracted (`ci.yml` `claude-config`). `scaffold.py` validates arguments with `fullmatch`. `gates.py` and `prereqs.py` use argument lists without `shell=True` (T-13, T-17). N-01 is a matching flaw, not injection.
- **V8 Authorization:** PASS-WITH-NOTES. T-03 is fixed and the T-12 guard is added. T-01, T-02, T-07 and N-04 remain guardrail gaps with follow-ups.
- **V13 Configuration:** PASS-WITH-NOTES. No secret was found in the diff (gitleaks). The Read denies are extended (T-05). T-04 residual, N-01 and N-02 have follow-ups.
- **V15 Secure coding and architecture:** PASS-WITH-NOTES. `dotnet-ef` is pinned exactly. Action and pre-commit pinning go to #28 (T-16). T-11 has a follow-up.
- **V16 Security logging and error handling:** PASS-WITH-NOTES. Gate verdict parsing is fixed (T-09a). Approvals (T-09b) and the hook audit log (T-18) have follow-ups.
- **V2 to V7, V9 to V12, V14, V17:** N/A. There is no application endpoint, session, token, OIDC, crypto, transport, data-protection or WebRTC change.
- **AI lanes:** N/A for the product. For the tooling, prompt injection is T-10 (accepted, and CLAUDE.md:26 is added).

## Follow-ups (none blocks this PR)

| Id | Proposed issue title | Covers | Blocks #35? |
| --- | --- | --- | --- |
| FU-1 | `chore(claude): run agents in the Claude Code sandbox (WSL2/devcontainer) with write and network-egress allow-lists and no secrets mounted` | T-01, T-02 (verify pattern-scoped `tools:` first), T-04 residual | No |
| FU-2 | `ci: run claude-config lint and gate scripts from the base ref, set job-level contents: read, require the check on main` | T-11, T-15 | No |
| FU-3 | Comment on #28 (`0.16 CI hardening`): `claude-review.yml: SHA-pin actions, load instructions/settings from the base ref and disable project hooks, require same-repo head, drop issues: write if unused; SHA-pin pre-commit repos` | T-12 remainder, T-16 | No, but the comment on #28 must exist before merge so the High has a linked follow-up |
| FU-4 | `chore(claude): bind gate skip approvals to Marco's GitHub identity (label or review checked in CI)` | T-09b | No |
| FU-5 | `chore(claude): harden hooks and gate checker (fail closed for listed agents, lint agents<->boundaries both ways, deny git --output, narrow gh issue/run, first-line verdict, hook audit log, stop-commands in changes job)` | T-06, T-07, T-14, T-18, N-04, N-05 | No |
| FU-6 | `fix(claude): anchor the secret_guard heredoc exemption and widen name matching` | N-01 residual (continuation, comment and quote forms; see re-check), N-02, N-03 | No |
