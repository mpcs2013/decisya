# Pipeline – issue #39: chore(claude): harden hooks and gate checker

- Branch: `issue/39-harden-hooks`
- Change class: ci-tooling
- Created: 2026-09-25
- Done when (from the issue): each of the 10 items in the issue body is fixed with a red/green test recorded here, `python .claude/scripts/lint.py` is green, a commit message that breaks CI's commitlint rules is rejected locally with CI's message, and `dotnet build -warnaserror` / `dotnet test` stay green.
- Scope (Marco, 2026-09-25): items 1-10 in the issue body; the parse-based sandbox lint (O-1) split into #52. Gate plan: ci-tooling plus G4; G1, G2 and G5 skipped.
- Decisions after G3 (Marco, 2026-09-25): the new package-fetching and destructive-command denies (items 4/5) bind project agents only, enforced by the hook on agent_type; the main session keeps asking for permission. `git --output` is denied for everyone. Local commitlint messages come from golden outputs generated once with the real commitlint 19 + config-conventional (npx into a temp folder; only the expected-output fixtures are committed).
- Host session (CLAUDE.md exception): the change is `.claude/**`, read-only inside the sandbox. G4 owner is the main session, because `.claude/boundaries.json` lets no project agent write `.claude/**` (by design); G3 and G6 are done by security-reviewer.

Gate rules and statuses: `.claude/skills/issue/SKILL.md`. Check with `python .claude/scripts/gates.py 39`.

| Gate | Owner | Status | Artifact | Note |
| --- | --- | --- | --- | --- |
| G0 | orchestrator | passed | docs/ai/pipeline/39.md | issue open; branch issue/39-harden-hooks; class ci-tooling |
| G1 | product-owner | skipped | | reason: class ci-tooling; no user stories, scope is the issue's item list; approved: Marco 2026-09-25 |
| G2 | architect | skipped | | reason: class ci-tooling; no architecture change (hardening of existing hooks and scripts); approved: Marco 2026-09-25 |
| G3 | security-reviewer | required | docs/security/threat-models/claude-hooks-hardening.md | |
| G4 | backend-dev / frontend-dev | required | | owner: main session (only it may write .claude/**) |
| G5 | test-engineer | skipped | | reason: class ci-tooling; no acceptance criteria to trace; approved: Marco 2026-09-25 |
| G6 | security-reviewer | required | docs/security/reviews/39.md | |
| G7 | orchestrator | required | | |

## G4 evidence

**Item 1 / G4-39-05 (hook path), found live (2026-09-25):** with the shell's working directory in a subfolder (`.claude/tests/fixtures/commitlint`), the hooks registered as `python .claude/hooks/...` could not find their scripts; every Bash, Write and Edit call was refused, including the edit that would fix it (a real instance of H-01, lockout, from a path bug rather than a deny). Marco edited `.claude/settings.json` by hand to `python "$CLAUDE_PROJECT_DIR/.claude/hooks/<hook>.py"`. Verified: a Bash call started in that subfolder ran normally; `head -1 .env.local` started in `docs/` was denied by the secret guard (hook works from any working directory and still denies).

## G7 PR body draft

