# Pipeline – issue #41: chore(claude): rootless Docker sidecar for Testcontainers in the agent sandbox

- Branch: `issue/41-docker-sidecar`
- Change class: ci-tooling
- Created: 2026-09-24
- Done when (from the issue): inside the sandbox, `dotnet test --filter-trait "Category=Integration"` runs against the sidecar (or a Testcontainers Postgres smoke test passes if no integration tests exist yet); nested containers cannot reach the internet or the Windows drives; the threat-model checks (G4-12, T-08 residual, registries only while the profile runs, rootless Podman evaluation) are recorded as evidence; runbook updated with Visual Studio 2026 | CLI steps.
- Gate plan (Marco, 2026-09-24): class ci-tooling plus G2 (sidecar design, Podman evaluation) and G4 (build and probes); G1 and G5 skipped.
- Decisions after G2 (Marco, 2026-09-24): rootless Podman in an unprivileged sidecar behind the opt-in overlay (`sandbox.py up --with-docker`), accepted as meeting G4-12(a) "off by default"; relaxations capped at the note's bounded set. **Fallback if the G4 spike fails: no sidecar** (option C; #41's Done-when then changes to host/CI integration tests) — no privileged container. Ryuk disabled. Pre-loaded images: Postgres only. Image digests: Dependabot if it tracks `images.Dockerfile`, otherwise a manual monthly bump. CLAUDE.md clarification for sandbox-infrastructure issues added in this PR.
- Working-agreement exception (Marco, 2026-09-24): agents work from the host session, not inside the sandbox, because this issue changes `.devcontainer/**`, which is read-only inside the sandbox. `host-review.py` and `git diff` before each commit still apply.

Gate rules and statuses: `.claude/skills/issue/SKILL.md`. Check with `python .claude/scripts/gates.py 41`.

| Gate | Owner | Status | Artifact | Note |
| --- | --- | --- | --- | --- |
| G0 | orchestrator | passed | docs/ai/pipeline/41.md | issue open; branch issue/41-docker-sidecar; class ci-tooling |
| G1 | product-owner | skipped | | reason: class ci-tooling; no user stories, scope is the issue's Done when; approved: Marco 2026-09-24 |
| G2 | architect | required | docs/architecture/agent-sandbox-docker-sidecar.md | sidecar design; builds on docs/architecture/agent-sandbox.md (#36) |
| G3 | security-reviewer | required | docs/security/threat-models/agent-sandbox-docker-sidecar.md | |
| G4 | backend-dev / frontend-dev | passed | | Podman spike passed within the ceiling; orchestrator's 14 engine probes, smoke test SELECT 1 => 1, 22 #36 probes, engine-off check; see G4 evidence |
| G5 | test-engineer | skipped | | reason: class ci-tooling; no acceptance criteria to trace; approved: Marco 2026-09-24 |
| G6 | security-reviewer | required | docs/security/reviews/41.md | |
| G7 | orchestrator | required | | |

## G4 evidence

**Spike (devops, evaluation of record): option B passed; stop rule not triggered.** Final relaxation set: cap_drop ALL + cap_add = Docker's default set exactly (incl. SETFCAP; never SYS_ADMIN); no-new-privileges off (rung 2, needed for newuidmap/newgidmap); custom seccomp = moby default (master 35797366, no v29.6.2 tag upstream) plus ALLOW for unshare, mount, umount2, setns, pivot_root, clone, clone3, sethostname, setdomainname, each with the recorded error in its comment; /dev/net/tun only; containers.conf netns=pasta, cgroups=disabled, keyring=false; read-only rootfs. Not needed: seccomp=unconfined (rung 4), pidns=host (5a, actively breaks with cgroups=disabled), systempaths=unconfined (5b), /dev/fuse (7), apparmor=unconfined (8; AppArmor inactive). keyctl stays blocked (keyring=false solved it). G4-41-11 (NETLINK_NETFILTER EPERM, should) not achievable as an addition-only change; deferred. Rootless DinD (A) confirmed to require --privileged (docker-library/docker README). Versions: Docker Desktop 4.83.0, Engine 29.6.2, kernel 6.6.87.2-microsoft-standard-WSL2, WSL 2.6.1.0, Podman 5.8.7, crun 1.28.

**Orchestrator's independent checks (2026-09-24):**
- Host: lint OK; `dotnet build -warnaserror` 0/0; unit + architecture lanes 76/76.
- `sandbox.py up --with-docker`: version floor printed, fresh engine, `docker.io/library/postgres:17-alpine` loaded; all three services healthy.
- docker inspect (sidecar): privileged false, user podman, read-only rootfs, CapAdd = Docker default set only, devices /dev/net/tun only, no PID/host namespace, no ports, only the two named engine volumes; `engine` network internal with exactly workspace + docker.
- Inside the sidecar (14/14 PASS): uid 1000, rootless true, CapEff 0, Seccomp 2, /proc/sys read-only, core_pattern write blocked, no /run/desktop, /mnt/host, /host_mnt, /mnt/c; no setuid/setgid binaries except newuidmap/newgidmap; registry pull blocked; nested container runs; nested container cannot reach example.com, 1.1.1.1, host.docker.internal or 192.168.65.1; nested `--privileged --security-opt seccomp=unconfined` still shows Seccomp 2; nested `--pid host` refused; nested mount of /run/desktop/mnt/host/c fails.
- **Done-when smoke test:** throwaway Testcontainers.PostgreSql 4.15.0 + Npgsql 10.0.3 project in the sandbox home (outside the repo; /tmp is noexec), Ryuk disabled: `host: docker; SELECT 1 => 1; 3.7s`; container removed afterwards; project deleted. The repo has no Category=Integration tests yet.
- #36 regression with the engine on: 22/22 probes PASS, 76/76 tests inside.
- Plain `sandbox.py up` afterwards: docker container removed, DOCKER_HOST empty, no /run/decisya-engine, `docker` not resolvable (engine off by default). Stack stopped.

**Note for Marco:** devops' host canary for section 6 set and then removed a dotnet user-secrets value (`Canary:Value`) without asking first; the store was never read (a `list` attempt was blocked by the secret guard).

## G7 PR body draft

