# Pipeline – issue #15: 0.03 Aspire AppHost + ServiceDefaults (OTel, health, JSON logs)

- Branch: `issue/15-apphost-servicedefaults`
- Change class: feature
- Created: 2026-09-25
- Done when (from the issue): Aspire dashboard shows a trace for a health call
- Decisions (Marco, 2026-09-25):
  - The health call is served by a new `src/Decisya.Api` skeleton (ServiceDefaults and health only; #20 adds JWT), not a throwaway host.
  - `/health` and `/alive` stay mapped in Development only; the deployment issue (0.17) decides production probes.
  - G1-G3 (docs) run in this host session. G4 and G5 run in the agent sandbox (ADR-0010): Marco starts `python .devcontainer/sandbox.py claude` and runs `/issue 15` there, then runs `.devcontainer/host-review.py` and reads `git diff` before G6 and G7 continue on the host. The dashboard check for the Done-when is done by Marco on the host, because the sandbox is headless.

Gate rules and statuses: `.claude/skills/issue/SKILL.md`. Check with `python .claude/scripts/gates.py 15`.

| Gate | Owner | Status | Artifact | Note |
| --- | --- | --- | --- | --- |
| G0 | orchestrator | passed | docs/ai/pipeline/15.md | issue open; branch issue/15-apphost-servicedefaults; class feature |
| G1 | product-owner | required | docs/requirements/phase-0/apphost-servicedefaults.md | |
| G2 | architect | required | docs/architecture/apphost-servicedefaults.md | |
| G3 | security-reviewer | required | docs/security/threat-models/apphost-servicedefaults.md | |
| G4 | backend-dev / frontend-dev | required | | |
| G5 | test-engineer | required | | |
| G6 | security-reviewer | required | | |
| G7 | orchestrator | required | | |

## Handover to the sandbox (G4, G5)

- G4 inputs:
  - the G1 requirements (Stories 1-5);
  - the G2 design;
  - the G3 threat model's "Changes to the G2 design" and G4-15-01 to 35. Where G3 and G2 differ, G3 wins.
- New package, with its justification: `Aspire.Hosting.Testing` 13.5.4, for the AppHost test project only.
- Two edits outside backend-dev's lane, made by the sandbox main session after backend-dev returns:
  - `.github/workflows/ci.yml`: add exactly one `--filter-not-trait "Category=AppHost"` to the unit-test step (G4-15-32).
  - `docs/GETTING-STARTED.md` step 2: replace the "Resources page stays empty" text with the dashboard steps from the G2 note (G1 decision 1).
- If an API behaves differently from the design, report the exact error and stop:
  - G4-15-19: `LogRecord.Exception` must be settable in OpenTelemetry 1.19.x.
  - G4-15-06: Aspire 13.5.4 must pass `OTEL_EXPORTER_OTLP_HEADERS` to `decisya-api`.
  - A `CS0122` on `Program`: the `public partial class Program;` fallback trips `CA1050`, which needs Marco's approval.
- The sandbox runs `dotnet build -warnaserror` and `dotnet test --no-build --filter-not-trait "Category=Integration" --filter-not-trait "Category=AppHost"`.
- Marco runs the AppHost test on the host. The manual dashboard check (the Done-when) follows the steps in the G2 note: a `GET /alive` trace for `decisya-api` in Traces, with correlated JSON console lines.

## Follow-ups from G2 and G3 (to record on issues after merge; nothing filed yet)

- #20: deny-by-default auth policy, generic client errors, span-attribute masking policy, filling the tenant and user log context, health endpoints kept anonymous.
- #22: Npgsql error detail and EF sensitive-data logging off outside Development (High there); widening the NetArchTest rules.
- #29 (0.17):
  - hash-key provisioning and rotation;
  - a pinned environment (Development never deployed);
  - production probes and `WithHttpHealthCheck`;
  - OTLP TLS and auth, and the collector as a PII store;
  - `AllowedHosts`.
- #18 and #19: strip browser `traceparent`, `tracestate` and `baggage` at the BFF.
- #28 or a new issue: run the AppHost tests in CI.
- New issue: a PII log-argument analyzer (F-7).
- Host session: the `otel-instrumentation` skill's "scopes carry TenantId" wording is out of date (`.claude/`, read-only in the sandbox).

## G4 evidence

## G7 PR body draft

