# Pipeline – issue #36: chore(claude): run agents in the Claude Code sandbox with write/egress allow-lists and no secrets mounted

- Branch: `issue/36-agent-sandbox`
- Change class: ci-tooling
- Created: 2026-09-23
- Done when (from the issue, updated 2026-09-23): agents can run sandboxed on Marco's Windows 11 machine (devcontainer, ADR-0010) in a headless terminal session; inside the sandbox, writes to host paths outside the repository and reads of secret files present at start fail, and network egress is limited to an allow-list Marco approved; `dotnet build -warnaserror` and the unit and architecture test lanes are green inside it (integration tests: #41); host build output lives outside the repository; a host-side review script flags host-executed files an agent changed; a runbook documents setup and use with Visual Studio 2026 | CLI steps.
- Decisions after G3 (Marco, 2026-09-23): headless agent sessions (G4-07 option A); dedicated Anthropic API key with a spend limit (G4-11); Docker sidecar (G4-12) split into #41; host bin/obj moved to %LOCALAPPDATA% (G4-05). Egress allow-list approved (Marco, 2026-09-23): api.nuget.org and api.anthropic.com on 443; NuGet certificate-revocation hosts on port 80 (GET only) limited to those the proxy deny log shows during `dotnet restore`, each recorded in G4 evidence; nothing else. Extended (Marco, 2026-09-24): platform.claude.com:443, documented as required by Claude Code (interactive startup connectivity check, no switch to disable it; interactive run failed with the proxy denial), plus CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 and DISABLE_AUTOUPDATER=1 so nothing else is attempted.
- Gate plan (Marco, 2026-09-23): class ci-tooling plus G2 (sandbox approach needs an architecture decision) and G4 (config is exercised: build and tests inside the sandbox).

Gate rules and statuses: `.claude/skills/issue/SKILL.md`. Check with `python .claude/scripts/gates.py 36`.

| Gate | Owner | Status | Artifact | Note |
| --- | --- | --- | --- | --- |
| G0 | orchestrator | passed | docs/ai/pipeline/36.md | issue open; branch issue/36-agent-sandbox; class ci-tooling |
| G1 | product-owner | skipped | | reason: class ci-tooling; no user stories, scope is the issue's Done when; approved: Marco 2026-09-23 |
| G2 | architect | required | docs/architecture/agent-sandbox.md | |
| G3 | security-reviewer | required | docs/security/threat-models/agent-sandbox.md | |
| G4 | backend-dev / frontend-dev | passed | | devops build + test-engineer RepoPaths fix; orchestrator's 22 probes, host/sandbox build 0/0 and 76/76, Marco's four checks; see G4 evidence |
| G5 | test-engineer | skipped | | reason: class ci-tooling; no acceptance criteria to trace; approved: Marco 2026-09-23 |
| G6 | security-reviewer | required | docs/security/reviews/36.md | |
| G7 | orchestrator | required | | |

## G4 evidence

Orchestrator's own checks, 2026-09-24 (independent of devops' report), stack started with `python .devcontainer/sandbox.py up` (precheck passed):

- **Host:** `dotnet build -warnaserror` 0 warnings / 0 errors; `dotnet test --filter-not-trait "Category=Integration"` 76/76; no bin/obj left in the working tree (output under %LOCALAPPDATA%\decisyartifacts). CI mapping (`GITHUB_ACTIONS=true`, reported by test-engineer): 76/76.
- **Inside workspace (22 probes, all PASS):** uid 1000 `vscode`, CapEff 0, NoNewPrivs 1, no sudo; writes to .claude/, CLAUDE.md, .git/, .devcontainer/, .pre-commit-config.yaml, global.json and /etc fail; /mnt/c not visible; writes under docs/ work; no key/token/secret/askpass/SSH/VS Code/browser variables in the environment.
- **Egress:** api.nuget.org 200 and api.anthropic.com reachable via the proxy; example.com, github.com, registry.npmjs.org denied; direct (no proxy), raw IP, host.docker.internal and DNS for outside names fail; SNI mismatch (example.com through api.nuget.org) terminated, matching SNI spliced.
- **Revocation (G4-10, Marco's rule):** first restore's deny log showed only CRL/OCSP GETs to 9 CA hosts plus www.microsoft.com/pkiops/ (and one .NET telemetry CONNECT, left denied; telemetry opted out). Added exactly those (microsoft limited to /pkiops/). After `dotnet nuget locals all --clear`: restore OK, 0 revocation denials, 0 NU3028/NU3037.
- **Build/test inside:** SDK 10.0.401; restore OK; `dotnet build -warnaserror` 0/0; unit + architecture lanes 76/76.
- **docker inspect:** workspace and egress both read-only rootfs, CapDrop ALL, no CapAdd, not privileged, no-new-privileges, pids/memory limits; workspace mounts = home and vscode-server volumes, tmpfs, the repository, the read-only overlays and the masked .vs; 0 mount sources matching docker.sock, user-secrets, .ssh or AppData.
- **Defect found and fixed during verification:** sandbox.py/precheck.py/host-review.py picked extensionless shims on Windows (WinError 193); now .exe/.com only.
- **Lint:** OK; sandbox-config check fires on a docker.sock mount added to compose.yaml.

**Marco's checks (all done, 2026-09-24):**
1. `.env` start canary: `sandbox.py up` refused while a `.env` file existed in the working tree and started after it was removed.
2. Mid-session canary: a `.env` created on the host while the stack ran was readable inside, as expected (documented residual R-4).
3. Headless and interactive Claude with the dedicated API key (U-26 verified). Two defects found and fixed on the way: managed `forceLoginMethod: "console"` pinned OAuth login in 2.1.273 (U-23 disproven, removed); interactive startup needs platform.claude.com (documented as required; added with Marco's approval). Workspace trust accepted once interactively (`~/.claude.json`: `/workspaces/decisya` trusted, checked by the orchestrator); `sandbox.py claude -p "reply with OK"` answers OK without the untrusted-workspace warning.
4. T-09 "no SNI" residual **accepted by Marco (2026-09-24)**: when a ClientHello carries no SNI, squid's `ssl::server_name` falls back to the allow-listed CONNECT host, so the connection can only reach that host; the SNI-mismatch case (the CDN-fronting attack) is terminated (verified). Recorded as an accepted residual for G6.

## G7 PR body draft

