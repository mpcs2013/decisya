# Pipeline – issue #59: chore(build): pre-push check for CI parity and package/MSBuild changes; fix commitlint mirror; disable VersionOverride

- Branch: `issue/59-pre-push-check`
- Change class: ci-tooling
- Created: 2026-09-26
- Done when (from the issue):
  - a capital-first subject is rejected locally with CI's exact message;
  - gitleaks versions match locally and in CI;
  - `git push` runs the pre-push check and blocks on failure;
  - a package or build-logic change prints a warning;
  - `VersionOverride` is rejected at build.
- Gate plan (Marco, 2026-09-26): ci-tooling plus G4, as in #39. G4 is implemented by the main session with red/green tests: `.claude/**` is outside every agent's lane, so no agent may write it.
- Runs on: host (ADR-0011; this issue changes `.claude/` tooling).

Gate rules and statuses: `.claude/skills/issue/SKILL.md`. Check with `python .claude/scripts/gates.py 59`.

| Gate | Owner | Status | Artifact | Note |
| --- | --- | --- | --- | --- |
| G0 | orchestrator | passed | docs/ai/pipeline/59.md | issue open; branch issue/59-pre-push-check; class ci-tooling |
| G1 | product-owner | skipped | | reason: class ci-tooling; no user stories; scope is the issue list; approved: Marco 2026-09-26 |
| G2 | architect | skipped | | reason: class ci-tooling; no architecture change; approved: Marco 2026-09-26 |
| G3 | security-reviewer | required | docs/security/threat-models/pre-push-ci-parity.md | |
| G4 | backend-dev / frontend-dev | passed | | owner: main session; four chunks red/green; build 0 warnings, 264/264 tests with CI filters |
| G5 | test-engineer | skipped | | reason: class ci-tooling; no acceptance criteria to trace; tests recorded in G4 evidence; approved: Marco 2026-09-26 |
| G6 | security-reviewer | required | docs/security/reviews/59.md | |
| G7 | orchestrator | required | | |

## G4 evidence

**Chunk A: commitlint mirror (G4-59-20 to 25), commit f8ce781.**
- 12 fixtures added: the #57 message verbatim and a first-word matrix. Their goldens were generated with the real @commitlint/cli 19.2.1 and config-conventional 19.1.0 (`make_golden.mjs`).
- The existing 29 goldens stayed byte-identical.
- The #57 golden matches CI's #57 log line for line (G4-59-21).
- Red, on HEAD's mirror: 4 fixtures failed: #57, `API x`, `Decisya.Api x`, `ApiClient x`.
- Fix: sentence-case upper-cases only the first character (G4-59-20). Green: 41 of 41 goldens.
- `ci.yml` pins wagoid to `v6.2.1`, and `fixtures/commitlint/source.json` records the tool versions; a test ties them together (G4-59-23).
- CI mirror-drift step (G4-59-24): the wagoid step is `continue-on-error`. The next step runs `commitlint.py --range base..head`, fails on a verdict mismatch ("commitlint mirror drift"), and otherwise enforces wagoid's outcome, so the gate is kept.
- `--range` validation plus `--end-of-options` (G4-59-25).
- Red on HEAD: 12 failures across the pin, range and golden tests. Green: 5 of 5 tests.

**Chunk B: CPM and NuGet audit policy (G4-59-13 to 16).**
- Before, recorded with a probe project in the repo:
  - `VersionOverride="13.0.3"` restored fine (exit 0), so projects could bypass central versions.
  - `VersionOverride="12.0.1"` failed with `NU1903: Warning As Error: … known high severity vulnerability`: the audit was already an error under `TreatWarningsAsErrors`, by SDK default (G4-59-16 record).
- Change:
  - `CentralPackageVersionOverrideEnabled=false` in `Directory.Packages.props`.
  - `NuGetAudit=true` and `NuGetAuditMode=all` set explicitly in `Directory.Build.props`.
  - New `Directory.Build.targets`: target `DecisyaCentralPackagePolicy`, `BeforeTargets=CollectPackageReferences;CollectPackageDownloads`.
- Green probes (restore):

  | Probe | Result |
  | --- | --- |
  | `VersionOverride` | `error NU1013` (verbatim: "The following PackageReference items cannot specify a value for VersionOverride") |
  | `.csproj` sets `CentralPackageVersionOverrideEnabled=true` | `DECISYA0002` |
  | `ManagePackageVersionsCentrally=false` + `Version=` | `DECISYA0001` |
  | `CentralPackageTransitivePinningEnabled=false` | `DECISYA0003` |
  | `PackageDownload` | `DECISYA0004` |
  | `NuGetAudit=false` | `DECISYA0005` |
  | a clean project | exit 0 |

- Red probes, with `-p:ImportDirectoryBuildTargets=false`:
  - re-enabling the override, `PackageDownload` and turning the audit off all restored fine (exit 0);
  - CPM off failed only incidentally, with `NU1015` from the version-less repo analyzer reference. `DECISYA0001` makes it explicit.
- `.claude/tests/test_build_policy.py` guards the properties, the five checks, and that no project sets `ImportDirectoryBuildTargets`. It fails 3 of 4 against HEAD's files.
- Solution: `dotnet build -warnaserror --no-incremental` gives 0 warnings; `dotnet test` (`Integration` excluded) passes 265 of 265.
- The probe project was removed with single-item deletes. A first cleanup command containing `rm -rf` was denied by the settings deny list and was not retried or worked around.

**Chunk C: gitleaks parity (G4-59-17 to 19), commit a941d25.**
- Checked against the docs first:
  - `gitleaks-action` README: "GITLEAKS_VERSION (optional): A particular Gitleaks version to use … Defaults to a hard-coded version number."
  - GitHub's Dependabot docs list a `pre-commit` ecosystem. It supports version updates, but not private repositories; this repository is PUBLIC (`gh repo view`).
- Both sides pinned to 8.30.1, the newest release, which matches the local binary: `.pre-commit-config.yaml` `rev: v8.30.1`, and `GITLEAKS_VERSION: "8.30.1"` on the `ci.yml` step.
- A full-history scan with 8.30.1 found no leaks.
- The pinned hook was built and run: `pre-commit run gitleaks --all-files` passed.
- Dependabot `pre-commit` ecosystem added (weekly).
- `test_gitleaks_parity.py`: green now. Red, in a temp copy of HEAD's files: "GITLEAKS_VERSION not set on the gitleaks-action step".
  - The first version of the test used a catastrophically backtracking regex and hung on the red input. Rewritten to scan only the step's lines; the red run now takes milliseconds.
- Disclosure: this commit was made with `SKIP=gitleaks`, because the pinned hook was building at the same time. The commit was then scanned with the same 8.30.1 binary: no leaks.
- Open until the PR's CI run: paste the gitleaks version line from the CI log (G4-59-17).

**Chunk D: pre-push check (G4-59-01 to 12), commit ae7d263.**
- `.claude/scripts/prepush.py`, wired as a `repo: local` hook at `stages: [pre-push]`.
- A second gitleaks hook entry on the same pinned repo runs `gitleaks git --redact --no-banner` at pre-push.
- `default_install_hook_types` now includes `pre-push`. The existing hooks got explicit stages (gitleaks and claude-lint: pre-commit; commitlint: commit-msg).
- `prereqs.py hooks` checks that all three hooks are pre-commit's shim.
  - Red on this clone before reinstalling: `MISSING git hook pre-push`.
  - After `pre-commit install`: three `ok` lines.
  - The issue skill's G0 step now runs it.
- `test_prepush.py`, 14 tests:
  - CI parity: filters and ignore regex parsed from `ci.yml`;
  - docs-only pushes skip the build;
  - path warnings, case-insensitive;
  - output escaping;
  - branch deletion is a no-op, and a missing `origin/main` fails closed;
  - the range comes from the merge-base, and a non-HEAD push reports "NOT RUN";
  - renames and deletions count, and `.csproj` token versus comment-only edits;
  - uncommitted tracked changes block the push;
  - a fake GitHub token built at run time is detected by the hook's exact `entry` and absent from the output;
  - the hooks check.
- End to end through pre-commit (`--hook-stage pre-push --from-ref origin/main --to-ref HEAD`):
  - with staged, uncommitted changes: blocked ("uncommitted tracked changes"), and the warning listed ci.yml, `.pre-commit-config.yaml`, `Directory.Build.props`, `Directory.Build.targets` and `Directory.Packages.props`;
  - on the clean committed tree: both hooks Passed. Run directly: build with 0 warnings, then 264 of 264 tests under CI's filters, then "prepush: OK".
- G4-59-12 is covered by the restore audit: NU1901-NU1904 are errors under `-warnaserror` (chunk B), so a vulnerable package fails the hook's build before the tests. No separate `--vulnerable` run was added. The warning text names that command for detail.
- Runbook `issue-pipeline.md`: install step (terminal only; VS 2026 has no UI for it) and a "Pre-push check" section with the guardrail wording and VS 2026 | CLI rows (G4-59-01, 02).

**Done-when, live:**
- `git commit --allow-empty -m "feat(api): Decisya.Api skeleton"` was rejected by the commit-msg hook with CI's text (`✖ subject must not be sentence-case, start-case, pascal-case, upper-case [subject-case]`); HEAD unchanged.
- gitleaks: 8.30.1 on both sides, with a parity test.
- `git push` runs the pre-push check and blocks on failure (shown above).
- A package or build-logic change prints the warning.
- `VersionOverride` is rejected with `NU1013`.
- Final: lint OK, the `.claude` suite OK, and the solution builds with 0 warnings.

## G7 PR body draft

