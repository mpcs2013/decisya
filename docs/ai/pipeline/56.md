# Pipeline – issue #56: docs(adr): host development by default; agent sandbox optional (ADR-0011)

- Branch: `issue/56-host-development`
- Change class: docs-only
- Created: 2026-09-26
- Done when (from the issue):
  - ADR-0011 is Accepted and linked from the ADR index;
  - ADR-0010 carries the "partly superseded" note;
  - CLAUDE.md and both runbooks state host-by-default with the optional sandbox;
  - the secret-guard docstring no longer relies on the sandbox boundary;
  - `python .claude/scripts/lint.py` is green.
- Gate plan (Marco, 2026-09-26): docs-only plus G2 and G3.
  - G2: the architect writes ADR-0011, the ADR-0010 status note, the index row and the runbook updates, with a short G2 note as the gate artifact.
  - G3: the security-reviewer writes the threat delta for dropping the sandbox as the default.
  - The main session edits `CLAUDE.md` and the `secret_guard.py` docstring (outside every agent's lane).
- G3 decisions (Marco, 2026-09-26):
  - Apply C1-C5. Deny the claude.ai connectors (C6).
  - File every follow-up:
    - #58 (FU-1, npm install and `.npmrc`);
    - #59 (FU-2, pre-push check, CI parity, commitlint mirror fix, `VersionOverride`);
    - #60 (FU-3, host/sandbox record);
    - #61 (FU-4, secret guard for host credential paths);
    - #62 (Postman collection from the OpenAPI contract, which replaces the live connector; starts with #20).
- Applied by the main session:
  - C1: `Directory.Packages.props` removed from the backend-dev and devops lanes.
  - C2: `permissions.ask` for Edit and Write of package files.
    - Live probe: Marco got the prompt for `/Directory.Packages.props`.
    - The `**/` form was added alongside; its probe was not observed.
  - C3: `Bash(npm ci*)` removed from the auto-allow list.
  - C4: Read denies for host credential paths.
    - Live probe: a read of a nonexistent file under `~/.ssh` was denied before any file access.
  - C5: `git status --porcelain --ignored` added to runbook step 7.
  - C6: `mcp__claude_ai_Claude_Docs`, `mcp__claude_ai_Postman` and `mcp__claude_ai_Microsoft_Learn` denied; the first two disconnected from the session at once.
  - `lint.py` and the `.claude` tests are green.
- Host session: this issue changes `.claude/` and the agent workflow itself, and runs on the host (its own subject).

Gate rules and statuses: `.claude/skills/issue/SKILL.md`. Check with `python .claude/scripts/gates.py 56`.

| Gate | Owner | Status | Artifact | Note |
| --- | --- | --- | --- | --- |
| G0 | orchestrator | passed | docs/ai/pipeline/56.md | issue open; branch issue/56-host-development; class docs-only |
| G1 | product-owner | skipped | | reason: class docs-only; no user stories; scope is the issue list; approved: Marco 2026-09-26 |
| G2 | architect | required | docs/architecture/host-development-default.md | ADR-0011 plus runbooks (Marco 2026-09-26) |
| G3 | security-reviewer | required | docs/security/threat-models/host-development-default.md | threat delta (Marco 2026-09-26) |
| G4 | backend-dev / frontend-dev | skipped | | reason: class docs-only; no code; approved: Marco 2026-09-26 |
| G5 | test-engineer | skipped | | reason: class docs-only; no acceptance criteria to trace; approved: Marco 2026-09-26 |
| G6 | security-reviewer | skipped | | reason: class docs-only; no code diff; G3 covers the risk; approved: Marco 2026-09-26 |
| G7 | orchestrator | passed | | PR body drafted below (Closes #56; no packages) |

## G4 evidence

## G7 PR body draft


**Title:** docs(adr): host development by default; agent sandbox optional (ADR-0011)

Makes host development the default, and the ADR-0010 agent sandbox optional. Marco decided this on 2026-09-26 after #15.

- ADR-0011 records the decision and its accepted risks:
  - [`docs/adr/0011-host-development-default.md`](docs/adr/0011-host-development-default.md)
  - Design note: `docs/architecture/host-development-default.md`
  - Threat delta: `docs/security/threat-models/host-development-default.md`
- ADR-0010 is marked "partly superseded"; its history is kept, and the ADR index is updated.
- The CLAUDE.md working agreement now says host by default. The sandbox is recommended when an issue brings in a new third-party package or feeds external content to an agent, and the manifest records `host` or `sandbox`.
- Runbooks:
  - `agent-sandbox.md` is marked optional, and `host-review.py` is needed only after a sandbox run.
  - `issue-pipeline.md` gains a "Where agents run" section and a pre-commit review step, including `git status --porcelain --ignored`.
- The secret-guard docstring no longer claims a sandbox boundary on the host.
- Config tightened to make up for the missing isolation (threat delta C1-C6):
  - C1: agents can no longer write `Directory.Packages.props`.
  - C2: editing package files (`Directory.Packages.props`, `NuGet.config`, `package.json`, the npm lockfile, `.npmrc`) asks Marco first. Checked live.
  - C3: `npm ci` is no longer auto-allowed.
  - C4: host credential files (Claude login, `~/.ssh`, git and GitHub CLI credentials) cannot be read. Checked live.
  - C6: the claude.ai connectors are denied in this repo.

Follow-ups:
- #58: npm install scripts; must land before the SPA (#26 is annotated).
- #59: pre-push CI-parity and package-change check, and the commitlint mirror fix.
- #60: record and check where the code gates ran.
- #61: secret guard for host credential paths.
- #62: Postman collections from the OpenAPI contract.

No NuGet, npm or .NET tool packages added.

Closes #56

🤖 Generated with [Claude Code](https://claude.com/claude-code)
